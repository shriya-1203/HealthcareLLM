Healthcare LLM is specialized in repsonding to health related quaeries 

1. Must have a .env file with GROQ_API_KEY=(instert your key here from groq.com)
2. download requirments with the command: pip install -r requirements.txt
3. to run the application, use the command: streamlit run main.py